{
  "routes": [
    {
      "method": "GET",
      "path": "/headings",
      "handler": "headings.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/headings/count",
      "handler": "headings.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/headings/:id",
      "handler": "headings.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/headings",
      "handler": "headings.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/headings/:id",
      "handler": "headings.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/headings/:id",
      "handler": "headings.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
